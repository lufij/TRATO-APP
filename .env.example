# Copia este archivo a `.env.local` y completa tus credenciales de Supabase
# IMPORTANTE: No agregues claves reales en el repositorio. Este archivo es
# solo un ejemplo. Mantén las claves sensibles (p.ej. `service_role`) fuera de
# archivos versionados y configúralas en tu proveedor de hosting (Vercel, CI).

# Supabase (obligatorio para build cliente)
# Valores de ejemplo: reemplaza con los de tu proyecto
VITE_SUPABASE_URL=https://TU_PROJECT_ID.supabase.co
VITE_SUPABASE_ANON_KEY=tu_anon_public_key

# Variables server-only (NO PONER en .env.local que se publique):
# SUPABASE_SERVICE_ROLE_KEY=tu_service_role_key  # debe guardarse solo en Vercel/CI como secret
# VERCEL_DIAG_SECRET=un_valor_fuerte_para_diagnostics_endpoint

# Google Maps (opcional)
# VITE_GOOGLE_MAPS_API_KEY=tu_api_key

# Variables para tests locales (opcional)
# TEST_EMAIL=usuario@ejemplo.com
# TEST_PASSWORD=tu_password
