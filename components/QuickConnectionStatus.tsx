import React from 'react';

// Minimal non-visual placeholder to satisfy imports without changing UI.
// It renders null but preserves props for future expansion.
export function QuickConnectionStatus(_props: { onShowDiagnostic?: () => void }) {
  return null;
}
